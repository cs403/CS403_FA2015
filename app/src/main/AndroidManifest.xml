<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.svsu.cs403.cs403_fa2015" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ActivitySelector"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare activities below here-->
        <activity
            android:name=".gpcorser_chap1"
            android:label="@string/gpcorser">
        </activity>
        <activity
            android:name=".gpcorser_chap2"
            android:label="@string/gpcorser">
        </activity>
        <activity
            android:name=".tskosask_chap1"
            android:label="@string/tskosask">
        </activity>

        <activity
            android:name=".tskosask_chap2"
            android:label="@string/tskosask">
        </activity>

        <activity
            android:name=".tskosask_chap3"
            android:label="@string/tskosask">
        </activity>

    </application>

</manifest>
